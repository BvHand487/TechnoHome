import React from 'react'

// 'active', 'enabled', 'functioning'

function Sensor({ name, status }) {

    return (
    <div>
        {
            
        }
    </div>
  )
}

export default Sensor